# Pip Installs Packages
That's what it stands for. It's a cli tool to install python packages.
# Errors
You don't have the command to run, `py -m ensurepip --default-pip` or `python3 -m ensurepip --default-pip`, then restart your terminal.