
Follow the instructions from [this page](https://www.python.org/downloads/).

If you're here for installing python modules via pip and are getting an error that you don't have the command to run, `py -m ensurepip --default-pip` or `python3 -m ensurepip --default-pip`, then restart your terminal.